# Python virtualenv & caches
.venv/
__pycache__/
*.pyc
*.pyo

# Model weights (ultralytics/HF download these on demand)
*.pt
*.pth
*.onnx
*.safetensors

# Pulled source videos + generated media (large binaries)
resources/videos/*
!resources/videos/.gitkeep

# Exports
*.zip

# Node
node_modules/

# Stray/unrelated large artifacts (not part of Card Beat)
mobileclip_blt.ts

# The Unity companion project has its own .gitignore (Library/Temp/Logs/etc.)

# OS / editor
.DS_Store
Thumbs.db

# Export outputs (generated frames/masks — regenerate from source, don't track)
resources/clips/
