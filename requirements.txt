# Card Beat desktop app + SAM segmentation backend.
# Python 3.10+ recommended (3.9 may work but newer torch/ultralytics prefer 3.10+).
pywebview>=5.0        # native desktop window hosting the editor UI
ultralytics>=8.3.0    # wraps SAM 3 / 3.1 and SAM 2 (video + text/point prompts)
torch>=2.2            # Apple Silicon: installs arm64 wheels with MPS support
opencv-python>=4.9    # video frame IO
numpy>=1.24
pillow>=10.0          # PNG mask writing
